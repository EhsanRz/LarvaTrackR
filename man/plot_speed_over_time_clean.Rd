% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_speed_over_time_clean.R
\name{plot_speed_over_time_clean}
\alias{plot_speed_over_time_clean}
\title{Plot cleaned speed over time}
\usage{
plot_speed_over_time_clean(
  tracks_movement_clean,
  individual = TRUE,
  show_sd = TRUE,
  max_speed = Inf
)
}
\arguments{
\item{tracks_movement_clean}{A data frame containing cleaned movement data.
It should contain \code{track_id}, \code{time_s}, and \code{speed_mm_s_clean} columns.}

\item{individual}{Logical. If \code{TRUE}, plots each track separately. If
\code{FALSE}, plots the mean speed across tracks. Default is \code{TRUE}.}

\item{show_sd}{Logical. If \code{TRUE}, adds a standard-deviation ribbon around
the mean speed when \code{individual = FALSE}. Default is \code{TRUE}.}

\item{max_speed}{Numeric. Maximum speed to include. Default is \code{Inf}.}
}
\value{
A \code{ggplot} object showing cleaned speed over time.
}
\description{
Creates a speed-over-time plot from cleaned movement-tracking data. The
function can either show individual tracks or summarise speed across tracks
at each time point.
}
\examples{
\dontrun{
plot_speed_over_time_clean(tracks_movement_clean)

plot_speed_over_time_clean(
  tracks_movement_clean,
  individual = FALSE
)
}

}

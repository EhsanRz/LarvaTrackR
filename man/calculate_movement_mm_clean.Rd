% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_movement_mm_clean.R
\name{calculate_movement_mm_clean}
\alias{calculate_movement_mm_clean}
\title{Calculate cleaned movement metrics in millimetres}
\usage{
calculate_movement_mm_clean(
  tracks,
  fps = 5,
  pixel_size_mm,
  min_step_px = 1,
  max_step_px = 25,
  remove_first_frames = 10
)
}
\arguments{
\item{tracks}{A data frame containing tracked positions. It should contain
\code{track_id}, \code{frame}, \code{x}, and \code{y} columns.}

\item{fps}{Numeric. Frames per second of the video. Default is \code{5}.}

\item{pixel_size_mm}{Numeric. Pixel size in millimetres per pixel, usually
calculated using \code{calibrate_scale()}.}

\item{min_step_px}{Numeric. Minimum movement step in pixels. Movements below
this value are treated as zero. Default is \code{1}.}

\item{max_step_px}{Numeric. Maximum allowed movement step in pixels.
Movements above this value are treated as missing. Default is \code{25}.}

\item{remove_first_frames}{Numeric. Number of early frames to exclude from
movement calculations. Default is \code{10}.}
}
\value{
A data frame with additional movement columns, including \code{time_s},
\code{x_mm}, \code{y_mm}, \code{dx_px}, \code{dy_px}, \code{movement_px_raw},
\code{movement_px_clean}, \code{movement_mm}, \code{speed_mm_s},
\code{total_distance_mm}, and \code{displacement_from_start_mm}.
}
\description{
Calculates frame-to-frame movement, speed, cumulative distance, and
displacement from the starting position. Small movements below a threshold
are treated as zero, large jumps above a threshold are treated as missing,
and early frames can be excluded from movement calculations.
}
\examples{
\dontrun{
tracks_movement <- calculate_movement_mm_clean(
  tracks,
  fps = 5,
  pixel_size_mm = pixel_size_mm,
  min_step_px = 1,
  max_step_px = 25,
  remove_first_frames = 10
)
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_tracks_3d.R
\name{plot_tracks_3d}
\alias{plot_tracks_3d}
\title{Plot tracked object paths in 3D over time}
\usage{
plot_tracks_3d(
  tracks,
  frames,
  frame_mats = NULL,
  fps = 5,
  first_frame = 1,
  last_frame = length(frames),
  image_downsample = 4,
  flip_y_for_display = TRUE
)
}
\arguments{
\item{tracks}{A data frame containing tracked positions. It should contain
\code{track_id}, \code{frame}, \code{x}, and \code{y} columns.}

\item{frames}{A list of image frames, usually loaded from a video.}

\item{frame_mats}{Optional list of precomputed grayscale image matrices. If
\code{NULL}, frames are converted internally using \code{get_gray_matrix()}.}

\item{fps}{Numeric. Frames per second of the video. Default is \code{5}.}

\item{first_frame}{Numeric. First frame to include in the 3D plot. Default is
\code{1}.}

\item{last_frame}{Numeric. Last frame to include in the 3D plot. Default is
\code{length(frames)}.}

\item{image_downsample}{Numeric. Downsampling factor for displaying image
planes. Higher values make the plot lighter and faster. Default is \code{4}.}

\item{flip_y_for_display}{Logical. If \code{TRUE}, flips y coordinates for display.
Default is \code{TRUE}.}
}
\value{
A \code{plotly} object showing tracked paths in 3D.
}
\description{
Creates an interactive 3D plot of object tracks across video frames using
\code{plotly}. The first and last video frames are shown as image planes, and
tracked object positions are plotted through time.
}
\examples{
\dontrun{
plot_tracks_3d(
  tracks,
  frames,
  fps = 5
)
}

}

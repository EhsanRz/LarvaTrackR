% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_objects_precise.R
\name{select_objects_precise}
\alias{select_objects_precise}
\title{Select objects precisely from a video frame}
\usage{
select_objects_precise(
  frames,
  frame_number = 1,
  n = NULL,
  zoom_size = 40,
  point_size = 0.6,
  rotate_180 = TRUE
)
}
\arguments{
\item{frames}{A list of image frames, usually loaded from a video.}

\item{frame_number}{Numeric. The frame number to use for object selection.
Default is \code{1}.}

\item{n}{Integer or \code{NULL}. Number of objects to select. If \code{NULL}, object
selection continues until the user stops clicking.}

\item{zoom_size}{Numeric. Half-width of the zoom window around the rough
clicked position, in pixels. Default is \code{40}.}

\item{point_size}{Numeric. Size of plotted selected points. Default is \code{0.6}.}

\item{rotate_180}{Logical. If \code{TRUE}, rotates the image by 180 degrees before
selection. Default is \code{TRUE}.}
}
\value{
A data frame with columns \code{selected_id}, \code{frame}, \code{x}, and \code{y}.
}
\description{
Displays a selected video frame and allows the user to manually select
objects by clicking roughly on each object, followed by a zoomed-in view for
more precise centre selection. The function returns the selected object
coordinates as a data frame.
}
\examples{
\dontrun{
selected_objects <- select_objects_precise(
  frames,
  frame_number = 1,
  n = 5
)
}

}

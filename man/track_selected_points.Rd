% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/track_selected_points.R
\name{track_selected_points}
\alias{track_selected_points}
\title{Track selected points across video frames}
\usage{
track_selected_points(
  frames,
  selected_objects,
  frame_mats = NULL,
  template_radius = 12,
  search_radius = 30,
  update_template = TRUE,
  flip_y = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{frames}{A list of image frames, usually loaded from a video.}

\item{selected_objects}{A data frame of manually selected objects. It should
contain \code{selected_id}, \code{frame}, \code{x}, and \code{y} columns.}

\item{frame_mats}{Optional list of precomputed grayscale image matrices. If
\code{NULL}, frames are converted internally using \code{get_gray_matrix()}.}

\item{template_radius}{Numeric. Radius of the template patch around each
selected object, in pixels. Default is \code{12}.}

\item{search_radius}{Numeric. Radius around the previous position to search
for the object in the next frame, in pixels. Default is \code{30}.}

\item{update_template}{Logical. If \code{TRUE}, the template is updated after each
tracked frame. Default is \code{TRUE}.}

\item{flip_y}{Logical. If \code{TRUE}, flips the selected y coordinates before
tracking. Default is \code{FALSE}.}

\item{verbose}{Logical. If \code{TRUE}, prints progress messages. Default is
\code{TRUE}.}
}
\value{
A data frame containing tracked object positions with columns
\code{track_id}, \code{frame}, \code{x}, \code{y}, and \code{score}.
}
\description{
Tracks manually selected objects across a sequence of video frames using
template matching. For each selected object, a small image patch is extracted
from the starting frame and searched in subsequent frames within a defined
search radius.
}
\examples{
\dontrun{
tracks <- track_selected_points(
  frames,
  selected_objects,
  template_radius = 12,
  search_radius = 30
)
}

}

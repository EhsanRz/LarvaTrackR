% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/track_selected_points_bright.R
\name{track_selected_points_bright}
\alias{track_selected_points_bright}
\title{Track bright selected objects across video frames}
\usage{
track_selected_points_bright(
  frames,
  selected_objects,
  frame_mats = NULL,
  search_radius = 35,
  threshold_quantile = 0.97,
  min_pixels = 3,
  max_jump_px = 40,
  verbose = TRUE
)
}
\arguments{
\item{frames}{A list of image frames, usually loaded from a video.}

\item{selected_objects}{A data frame of manually selected objects. It should
contain \code{selected_id}, \code{frame}, \code{x}, and \code{y} columns.}

\item{frame_mats}{Optional list of precomputed grayscale image matrices. If
\code{NULL}, frames are converted internally using \code{get_gray_matrix()}.}

\item{search_radius}{Numeric. Radius around the previous object position to
search in the next frame, in pixels. Default is \code{35}.}

\item{threshold_quantile}{Numeric. Quantile used to define bright pixels
within the local search region. Default is \code{0.97}.}

\item{min_pixels}{Integer. Minimum number of bright pixels required to update
the object position. Default is \code{3}.}

\item{max_jump_px}{Numeric. Maximum allowed jump between consecutive frames,
in pixels. Larger jumps are rejected. Default is \code{40}.}

\item{verbose}{Logical. If \code{TRUE}, prints progress messages. Default is
\code{TRUE}.}
}
\value{
A data frame containing tracked object positions with columns
\code{track_id}, \code{frame}, \code{x}, \code{y}, \code{n_pixels}, and \code{jump_px}.
}
\description{
Tracks manually selected bright objects across a sequence of video frames.
For each selected object, the function searches within a local radius around
the previous position, detects the brightest pixels using a quantile-based
threshold, and uses their centroid as the new object position.
}
\details{
This method is useful when the tracked objects are brighter than the
surrounding background.
}
\examples{
\dontrun{
tracks_bright <- track_selected_points_bright(
  frames,
  selected_objects,
  search_radius = 35,
  threshold_quantile = 0.97
)
}

}

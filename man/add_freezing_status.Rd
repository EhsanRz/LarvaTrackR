% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_freezing_status.R
\name{add_freezing_status}
\alias{add_freezing_status}
\title{Add freezing status to movement tracks}
\usage{
add_freezing_status(
  tracks_movement,
  speed_threshold = 0.2,
  min_freeze_duration_s = 2,
  fps = 5,
  speed_col = "speed_mm_s_clean"
)
}
\arguments{
\item{tracks_movement}{A data frame containing movement data. It should
contain \code{track_id}, \code{frame}, and a speed column.}

\item{speed_threshold}{Numeric. Speed threshold below which an object is
considered slow. Default is \code{0.2}.}

\item{min_freeze_duration_s}{Numeric. Minimum duration in seconds required
to define a freezing bout. Default is \code{2}.}

\item{fps}{Numeric. Frames per second of the video. Default is \code{5}.}

\item{speed_col}{Character. Name of the speed column used for freezing
detection. Default is \code{"speed_mm_s_clean"}.}
}
\value{
A data frame with additional columns \code{is_slow}, \code{freeze_group},
\code{bout_length_frames}, and \code{freezing}.
}
\description{
Identifies freezing periods based on a speed threshold and a minimum freezing
bout duration. A frame is classified as freezing only when the object remains
below the speed threshold for at least the specified duration.
}
\examples{
\dontrun{
tracks_freezing <- add_freezing_status(
  tracks_movement,
  speed_threshold = 0.2,
  min_freeze_duration_s = 2,
  fps = 5,
  speed_col = "speed_mm_s"
)
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_video_frames.R
\name{read_video_frames}
\alias{read_video_frames}
\title{Read video frames into R}
\usage{
read_video_frames(
  video_path,
  output_dir = tempfile("video_frames_"),
  fps = 5,
  format = "png",
  start_time = 0,
  duration = NULL
)
}
\arguments{
\item{video_path}{Character. Path to the input video file.}

\item{output_dir}{Character. Directory where extracted frame images will be
saved. Default is a temporary directory.}

\item{fps}{Numeric. Frames per second to extract from the video. Default is
\code{5}.}

\item{format}{Character. Image format for extracted frames. Default is
\code{"png"}.}

\item{start_time}{Numeric. Start time in seconds for frame extraction.
Default is \code{0}.}

\item{duration}{Numeric or \code{NULL}. Duration in seconds to extract from the
video. If \code{NULL}, frames are extracted from the full video.}
}
\value{
A list of EBImage image objects. The list also contains attributes:
\code{frame_files}, \code{output_dir}, \code{fps}, \code{start_time}, and \code{duration}.
}
\description{
Extracts frames from a video file at a defined frame rate and loads them as
EBImage image objects. The extracted frame files are saved in an output
directory, and the loaded frames are returned as a list.
}
\examples{
\dontrun{
frames <- read_video_frames(
  video_path = "example_video.mp4",
  fps = 5
)
}

}
